void setup(){
  size(500,500);
  rectMode(CENTER);
}
float f;
void draw(){
  translate(mouseX,mouseY);
  rotate(f);
  f=f+0.05; // 회전속도
  fill(0,255,0); //사각형 배경색
  rect(0,0, 80,80);
  fill(255,0,255); // 타원 배경색
  ellipse(0,0,80,80);
}
